import { ReactNode } from "react";
import { ThemeModeEnum } from "../enums/themeModeEnum";
interface Props {
    children: ReactNode;
    theme?: object;
    mode?: ThemeModeEnum;
}
export declare const ThemeProvider: ({ children, theme, mode }: Props) => JSX.Element;
export {};
